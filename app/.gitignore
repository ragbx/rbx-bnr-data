# Sortie partagée des builds PyInstaller (ead_editor + ead_prepublication),
# produite via `pyinstaller ... --distpath ../dist` depuis chaque app.
# Jamais commitée : le CI publie les .exe comme assets de release GitHub
# (cf. workflows build-windows*.yml) plutôt que de les versionner ici.
/dist/

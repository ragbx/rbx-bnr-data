# Scripts : S3

**Emplacement :** `scripts/s3/`

Scripts de versement, d'inventaire et de suppression des
[fichiers de conservation](../donnees/fichiers.md) sur le stockage S3.

## Pourquoi utiliser un script et non un utilitaire type Cyberduck ?
On verse les fichiers dans le S3 avec deux métadonnées d'identification :
- `uuid`
- `checksum_md5`
Ces deux métadonnées permettent d'identifier sûrement le fichier et de vérifier son intégrité.
Or les utilitaires disponibles (Cyberduck, ...) ne permettent pas l'envoi de ces informations.

---

## Configuration

La connexion est définie dans `scripts/s3/conf.yml` (non versionné, voir le
modèle `conf_empty.yml`) : URL du endpoint, région et identifiants pour trois
profils d'accès :

| Profil | Usage |
|---|---|
| `user_ro` | Lecture seule (profil par défaut) |
| `user_rw` | Lecture/écriture (versement, suppression) |
| `user_test` | Tests |

Le module `rbx_s3.py` encapsule la connexion boto3 et expose deux classes :
- `Rbx_client` : client S3, avec une méthode `upload()` qui verse un fichier
  puis vérifie sa présence et sa taille (`head_object`) ;
- `Rbx_resource` : ressource S3, avec une méthode `delete_object()`.

---

## Versement : `upload.py` / `upload_incommunicable.py`

Versent des lots de fichiers vers le bucket communicable (`upload.py`) ou
incommunicable (`upload_incommunicable.py`).

- **Entrée** : fichiers CSV déposés dans `results/s3/transfert/` (`input_folder`
  en tête de script), listant les fichiers à verser (colonnes `name`, `path`,
  `uuid`, `checksum_md5`, `size`, `s3_key`). Les chemins sont résolus depuis le
  partage réseau ou disque défini par la variable `prefix` en tête de script
  (à adapter à chaque lot : ce chemin est en dur, propre au poste/support
  utilisé pour le versement).
- **Traitement** : versement en parallèle (`ThreadPoolExecutor`). Chaque objet
  reçoit les tags S3 `uuid` et `checksum_md5`. Après versement, la taille de
  l'objet est comparée à la taille attendue.
- **Sortie** : un journal CSV par lot dans `results/s3/result/`
  (`{lot}_upload_{horodatage}.csv`), avec le résultat et l'erreur éventuelle
  de chaque fichier.

---

## Inventaire : `list_all.py`, `list2.py` à `list8.py`

Listent les objets d'un bucket avec leurs métadonnées (clé, date de modification,
taille, classe de stockage) et leurs tags `uuid` et `checksum_md5`. Ce sont des
scripts ponctuels (copiés-collés au fil des besoins), à variante quasi
identique : seuls le bucket et la liste de préfixes filtrés changent. Sortie en
tranches de 10 000 objets, dans `data/s3/listing{n}-{bucket}_{préfixe}_{n}.csv.gz`
(sauf `list_all.py`, voir plus bas).

| Script | Bucket | Préfixes filtrés |
|---|---|---|
| `list_all.py` | communicable | tout le bucket (pas de filtre) — sortie dans `result/listing-{bucket}_all_{n}.csv.gz` |
| `list2.py` | communicable | `CSV`, `LAI`, `LAR`, `MDF`, `MUS`, `OBS`, `PAR`, `TCG` |
| `list3.py` | communicable | `AMR_2I`, `AMR_6H`, `AMR_AFF`, `AMR_AVI`, `AMR_CIM`, `AMR_DEL`, `AMR_Obj`, `AMR_PHO`, `AMR_POP`, `AMR_PR`, `AMR_RAM`, `PAR_LAB` |
| `list4.py` | communicable | `MED_AFF`, `MED_AVI`, `MED_CHA`, `MED_CP`, `MED_FLRS`, `MED_IMA`, `MED_JOU`, `MED_LET`, `MED_MON`, `MED_MS`, `MED_PER`, `MED_PHO`, `MED_VDM` |
| `list5.py` | communicable | `MED_PRA/PRA_RTG`, `PRA_CTG`, `PRA_IND`, `PRA_CRT`, `PRA_AVE`, `PRA_ERT`, `PRA_JRX` |
| `list6.py` | **incommunicable** | `AMR_EC` |
| `list7.py` | communicable | `AMR_GUE` |
| `list8.py` | communicable | `AMR_CAD`, `AMR_PR`, `ARA_CPS`, `LAR_PUB`, `MED_CP`, `MED_EPH`, `MED_FOO`, `MED_IMA`, `MED_MAR`, `MED_MON`, `MED_PHO`, `MED_PLA`, `MED_PUB` |

`list3.py` à `list8.py` ciblent chacun un lot de corpus précis (vérification
post-versement ou post-keying `s3_key_cible` d'un corpus donné) — la liste de
préfixes n'est donc pas figée, elle évolue à chaque nouveau lot à contrôler.

Les listings consolidés sont conservés dans `data/s3/s3_listing_{date}.csv.gz`.

---

## Analyse : `repertoires_traitement_s3_ko.py`

Liste les répertoires contenant des fichiers dont le traitement S3 n'est pas
réalisé. Voir [sa page dédiée](repertoires_traitement_s3_ko.md).

---

## Suppression : `delete.py`

Supprime du bucket les objets dont les clés sont listées dans un CSV
(colonne `key`). Le chemin du CSV et le bucket sont définis en tête de script.

**À manier avec précaution** : la suppression est immédiate et sans confirmation.

---

## Voir aussi

- [Campagne s3_key_cible](s3_key_cible.md) — scripts ponctuels `scripts/s3/*_s3_key_cible.py` et `scripts/s3/ref_*_20260711.py` qui calculent la clé cible des corpus hors pipeline standard et l'intègrent au référentiel
